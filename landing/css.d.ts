// Ambient declarations for side-effect CSS imports (e.g.
// `import 'nextra-theme-docs/style.css'`). Next.js bundles these at build time,
// but TypeScript needs a module declaration so the editor doesn't flag them.
declare module '*.css';
